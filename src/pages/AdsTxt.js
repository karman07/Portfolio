import React from "react";

const AdsTxt = () => {
  const adsTxtContent = `google.com, pub-7069180478740738, DIRECT, f08c47fec0942fa0`;

  return (
    <div style={{ whiteSpace: "pre-wrap", fontFamily: "monospace", padding: "1rem" }}>
      {adsTxtContent}
    </div>
  );
};

export default AdsTxt;
